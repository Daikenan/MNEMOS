# Mnemos 环境变量样例
# 使用方式：复制为 .env 并填入真实值
#   cp env.example .env

# ----------------------------------------
# MemOS 云端 (LTM 持久化)
# ----------------------------------------
MEMOS_API_KEY=mpg-xxx
MEMOS_BASE_URL=https://api.memos.example.com

# ----------------------------------------
# OpenRouter（决策层 / 数据层 LLM）
# ----------------------------------------
OPENROUTER_API_KEY=sk-or-v1-xxx

# ----------------------------------------
# KnowMe-Bench 判分器（二选一即可）
# 不填 OPENAI_API_KEY 时，将用 OPENROUTER_API_KEY + openai/gpt-4o 判分
# ----------------------------------------
# OPENAI_API_KEY=sk-xxx
